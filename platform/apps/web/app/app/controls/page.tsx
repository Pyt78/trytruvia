export default function ControlsPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold tracking-[-0.04em]">Controls</h1>
      <p className="max-w-xl text-sm text-slate-500">The shared control library and its continuous tests. One control satisfies requirements across every framework it maps to.</p>
      <p className="inline-flex rounded-full bg-slate-100 px-3 py-1 text-xs font-medium text-slate-600">
        Phase 1
      </p>
    </div>
  );
}
